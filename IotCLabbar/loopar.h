typedef enum
{
	SKRIVUT_ERRORCODE_OK,
	SKRIVUT_ERRORCODE_N_TOO_LARGE,
	SKRIVUT_ERRORCODE_TEMP_TOO_LOW
}SKRIVUT_ERRORCODE;

typedef enum
{
	GOALIE,
	DEFENCE,
	FORWARD

}PLAYER_POSITION;

void PrintPlayer(char* namn, PLAYER_POSITION pos);

